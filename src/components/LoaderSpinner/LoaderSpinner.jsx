import React from 'react'
import classes from "./loader-spinner.module.css"

const LoaderSpinner = () => {
    return (
        <span className={classes.loader}></span>
    )
}

export default LoaderSpinner
